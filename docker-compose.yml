services:
  web:
    build: .
    image: finance-hub:latest
    container_name: finance-hub
    ports:
      - "3002:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_PATH=./data/finance-hub.db
      - AUTH_PASSWORD=${AUTH_PASSWORD}
    restart: unless-stopped
